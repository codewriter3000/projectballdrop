import 'dart:ui';

import 'package:flame/flame.dart';
import 'package:flame/game.dart';

import 'game_controller.dart';

class Scales {
  /*int getHeightFromDecimal(double decimal){
    if (decimal.ceil() > 1){
      throw new Exception();
    } else {
      return (decimal * GameController.screenSize.height).round();
      //return decimal * height of screen
    }
  }

  int getWidthFromDecimal(double decimal){
    if (decimal.ceil() > 1){
      throw new Exception();
    } else {
      //return decimal * width of screen
    }
  }

  double getDecimalFromHeight(int height){
//return height / height of screen
  }

  double getDecimalFromWidth(int width){
    //return width / width of screen
  }*/
}